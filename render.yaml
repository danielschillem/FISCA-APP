# render.yaml — FISCA Backend + Frontend
# https://render.com/docs/yaml-spec
#
# Base de données : Neon PostgreSQL (gratuit, déjà créé)
#
# ── PROCÉDURE DE DÉPLOIEMENT ──────────────────────────────────────────────────
# 1. Render Dashboard → New → Blueprint → repo danielschillem/FISCA-APP
# 2. Render crée fisca-backend + fisca-frontend
# 3. fisca-backend → Environment → ajouter :
#    DATABASE_URL = <URL Neon PostgreSQL>
#    OPENAI_API_KEY = sk-... (optionnel, pour l'assistant IA)
# 4. fisca-frontend → Settings → Custom Domain (ex: app.fisca.bf)
# 5. fisca-backend → Settings → Deploy Hook URL
#    → GitHub Secrets → RENDER_DEPLOY_HOOK_BACKEND

services:
  # ── Backend Go ────────────────────────────────────────────────────────────
  - type: web
    name: fisca-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    region: frankfurt
    plan: free
    autoDeploy: true
    branch: main
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        sync: false        # → coller l'URL Neon manuellement
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: "8080"
      - key: ALLOWED_ORIGIN
        sync: false        # → ex: https://fisca-frontend.onrender.com,https://app.fisca.bf
      - key: ADMIN_SECRET
        sync: false
      - key: SUPERADMIN_EMAIL
        sync: false        # → email du super administrateur FISCA
      - key: SUPERADMIN_PASSWORD
        sync: false        # → mot de passe du super administrateur
      - key: OPENAI_API_KEY
        sync: false        # → clé API OpenAI pour l'assistant IA (optionnel)
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        sync: false
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: SMTP_FROM
        sync: false

  # ── Frontend Vite (nginx) ─────────────────────────────────────────────────
  - type: web
    name: fisca-frontend
    runtime: docker
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    region: frankfurt
    plan: free
    autoDeploy: true
    branch: main
    envVars:
      - key: VITE_API_URL
        fromService:
          name: fisca-backend
          type: web
          property: host
          transform: "https://{value}/api"

